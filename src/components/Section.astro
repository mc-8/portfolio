---
interface Props {
  title?: string
}

const { title } = Astro.props
---

<section class="flex flex-col gap-8 mb-10">
  {title && <h2 class="font-black text-2xl md:text-4xl">{title}</h2>}
  <div>
    <slot />
  </div>
</section>
